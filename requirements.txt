# Core API + data (required for the structured endpoints and tests)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2

# LLM + RAG stack (required for /chat and the demo)
langchain-core>=0.3
langchain-anthropic>=0.3
langgraph>=0.2
chromadb>=0.5

# Dev / test
pytest>=8.0
httpx>=0.27
